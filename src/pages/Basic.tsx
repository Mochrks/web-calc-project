
import { BasicCalc } from '@/components/demo/BasicCalc'
import { Layout } from '@/layout'

export const Basic = () => {
    return (
        <Layout>
            <BasicCalc />
        </Layout>
    )
}