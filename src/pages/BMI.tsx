
import { BMICalc } from '@/components/demo/BmiCalc'
import { Layout } from '@/layout'

export const BMI = () => {
    return (
        <Layout>
            <BMICalc />
        </Layout>
    )
}