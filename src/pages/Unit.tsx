import { UnitConverter } from "@/components/demo/UnitConverter"
import { Layout } from '@/layout'

export const Unit = () => {
    return (
        <Layout>
            <UnitConverter />
        </Layout>
    )
}
