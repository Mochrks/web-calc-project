
import { HomePage } from '@/components/demo/HomePage'
import { Layout } from '@/layout'

export const Home = () => {
    return (
        <Layout>
            <HomePage />
        </Layout>
    )
}